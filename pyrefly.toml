# Pyrefly configuration — points the type checker to the project's virtual environment
# so that fastapi, pydantic, pdfplumber, etc. resolve correctly.

python_interpreter = ".venv/bin/python"

[python]
site_package_path = [
  ".venv/lib/python3.9/site-packages",
]
